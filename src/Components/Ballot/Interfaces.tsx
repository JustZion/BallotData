export interface dataList {
    id: string,
    items: {
      id: string,
      photoUrL: string,
      title: string
    }[]
    title: string
  }

 export interface dataArray {
    id: string,
    photoUrL: string,
    title: string    
}
  
  

